name: CI - DevOps Assignment

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: |
          npm install

      - name: Build Docker image
        run: |
          docker build -t hello-pavans:latest .

      - name: Run Docker container
        run: |
          docker run -d -p 9090:9090 --name hello-pavans-container hello-pavans:latest
          sleep 5

      - name: Test /hello endpoint
        run: |
          curl --fail http://localhost:9090/hello

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Tag image
        run: docker tag hello-pavans ${{ secrets.DOCKERHUB_USERNAME }}/hello-pavans:latest

      - name: Push image to Docker Hub
        run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/hello-pavans:latest
